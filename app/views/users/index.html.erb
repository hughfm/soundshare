<h1>listing all users</h1>

<%= link_to 'new user', new_user_path %>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.name, user %> | <%= link_to 'delete', user, method: :delete %></li>
  <% end %>
</ul>
